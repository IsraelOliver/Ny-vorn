1 - Coloca no JSON health > maxHealth e valida que na inicialização o clamp mantém Health <= MaxHealth -- Idem para DarkEnergy vs MaxDarkEnergy.

2 - EnemyConfig + JSON: extrair AggroDistance, Walk/ChaseSpeed, JumpSpeed, ContactDamage, I-frames, etc. (o impacto em tuning vai ser enorme).

3 - World preset JSON: seed, dimensions, baseline/amplitude, escalas e thresholds

4 - Animation metadata JSON: tempos por estado e, se quiser, dimensões/linhas/colunas.